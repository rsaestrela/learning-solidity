pragma solidity ^0.7.4;

contract Simple {
    // Arithmetic operators + - * / % **
    uint a = 2;
    utin b = 3;
    
    uint c = a ** b;
    
    // logical operators
    // !  && || == !=
    bool hasMoney = !false;
    
    // Bitwise operators
    // & | ^ ~ >> <<
    bytes1 contractValue = 0x02 | 0x01;
    
}